replicaCount: 1

image:
  repository: ghcr.io/alun-hub/sudo-logserver
  pullPolicy: IfNotPresent
  tag: "1.0"

storage:
  type: local # local | distributed

tls:
  mode: self-signed # self-signed | provided | cert-manager
  existingSecret: ""

postgresql:
  enabled: false

minio:
  enabled: false
